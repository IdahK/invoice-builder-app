spring:
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:gakiidah73@gmail.com}
    password: ${SMTP_PASSWORD:6dt+=^>=,jEb,%>HVk?f}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

notifications:
  email:
    provider: ${EMAIL_PROVIDER:smtp}
  sendgrid:
    api-key: ${SENDGRID_API_KEY:}
